let nums = [1, 7, 7];

let userName = {
    email: 'khan@mail.ru',
    name: 'khan'
};

if (userName.email === 'khan@mail.ru') {
    userName.isAdmin = true;
    userName.id = nums;
}

// tested how to push/add new property to an object with 'if' statement