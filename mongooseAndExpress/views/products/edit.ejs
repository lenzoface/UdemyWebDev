<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
</head>
<body>
    <h1>Add a Product</h1>
    <form action="/products/<%= product._id %>?_method=PUT" method="POST">
        <label for="name">Product Name</label>
        <input type="text" name="name" placeholder="product name" value="<%= product.name %> " id="name"><br> <hr>
        <label for="price">Price</label>
        <input type="number" name="price" placeholder="price" value="<%= product.price %>" id="name"><br><hr>
        <label for="category">Category</label>
        <select name="category" id="category" value="<%= product.category %>">
            <% for(let category of categories) { %>
                <option value="<%=category%>" <%= product.category === category ? 'selected': ''%>><%=category%></option>
                <% } %> 
        </select>
        <br> <hr>
        <button>Submit!</button>
        <hr>
        <a href="/products/<%= product._id %>">Cancel editing</a> <hr>
        <a href="/products">Back to All Products!</a>   
    </form>
</body>
</html>